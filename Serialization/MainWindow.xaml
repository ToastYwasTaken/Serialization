<Window x:Class="Serialization.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Serialization" d:DataContext="{d:DesignInstance Type=local:LevelVM}"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <StackPanel>
        <Label>Name</Label>
        <TextBox Text="{Binding Name}"></TextBox>
        <Label>Width</Label>
        <TextBox Text="{Binding Width}"></TextBox>
        <Label>Height</Label>
        <TextBox Text="{Binding Height}"></TextBox>
        <Button Content="SaveBtn" Click="Button_Click"></Button>
        <Button Content="LoadBtn" Click="Button_Click_1"></Button>
    </StackPanel>
</Window>
